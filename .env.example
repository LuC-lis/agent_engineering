# 配置模板 —— 复制成 .env 后填入真实值：
#     cp .env.example .env
#
# .env 已被 .gitignore 忽略，不会进仓库。
# 真实环境变量优先级高于本文件，因此 CI / 服务器上可以直接 export。

# LLM 的 chat completions 端点（注意要写完整路径，只写到 /v1 会 404）
API_URL=https://api.deepseek.com/v1/chat/completions

# 你的 API Key
API_KEY=sk-xxxxxxxx

# 模型名
MODEL=deepseek-flash
